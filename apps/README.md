# Applications

Runnable entry points live here:

- `server/` — local control-plane API and background processing;
- `web/` — browser-based local user interface;
- `desktop/` — optional desktop wrapper;
- `cli/` — command-line interface and automation entry point.

The implementation stack is intentionally undecided at this stage.
